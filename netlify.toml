[build]
  command = "npm run build"
  publish = ".next"

# Official Next.js runtime for Netlify — handles SSR, API routes,
# and page rendering as serverless functions.
[[plugins]]
  package = "@netlify/plugin-nextjs"
